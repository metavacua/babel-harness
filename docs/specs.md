# Specs

Index of durable component specs for `babel-harness`. Unlike
`larql-to-sparql` (a multi-crate workspace where specs live beside each
crate), `babel-harness` is a single cohesive package, so specs live flat
under `docs/specs/` rather than per-component.

| Spec | Path |
|------|------|
| Claude Code × Pi Agent Integration | [docs/specs/2026-06-23-design.md](specs/2026-06-23-design.md) |
